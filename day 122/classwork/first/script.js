class Quadrilateral {
    constructor(length, width) {
        this.length = length;
        this.width = width;
    }
    get Area(){
        return `სიგრძე ${this.length} სიგანე ${this.width}`
    }
}

